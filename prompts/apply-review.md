# Apply CQB Review

Reviewer output is engineering advice, not repository truth. Before editing, validate each important assumption against current files and runtime evidence. Reject contradicted recommendations, gather only the missing evidence required, choose the smallest valid change, run relevant verification, inspect the diff, and report the structured result to CQB.
